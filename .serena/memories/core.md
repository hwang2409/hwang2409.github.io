# Core

- Personal site at `/Users/henry/me/fun/website`.
- App Router pages live under `src/app`: home, blog index, blog slug pages, resume.
- Blog markdown lives in `content/blog/*.md`; rendering pipeline is in `src/lib/blog.ts`.
- Shared shell/navigation/theme lives in `src/app/layout.tsx` and `src/components/SiteNav.tsx` / `src/components/ThemeToggle.tsx`.
- Minimal static document aesthetic is intentional: Consolas only, light by default, text-only controls, no icons, no dynamic backgrounds.
- For stack and commands read `mem:tech_stack` and `mem:suggested_commands`; for local style rules read `mem:conventions`; for completion checks read `mem:task_completion`.