# Suggested Commands

- Install deps: `npm install`.
- Dev server: `npm run dev -- -p 3005`.
- Production/static export check: `npm run build`.
- Lint/type-aware Next checks: `npm run lint`.
- Push deploy branch: `git push origin main`.
- Serena memory sanity check, if memories are edited: `serena memories check` from project root.